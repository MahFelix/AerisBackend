# Configuração do PostgreSQL
spring.datasource.url=jdbc:postgresql://dpg-cuubs3d8L47c73984ap9-a:5432/nutricionista
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuração do JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuração do CORS
spring.webflux.cors.allowed-origins=https://aeris-smart.netlify.app
spring.webflux.cors.allowed-methods=GET,POST
spring.webflux.cors.allowed-headers=Content-Type

server.port=8090
